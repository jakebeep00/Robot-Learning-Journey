
## 📤 **1. 파일 올리기 (기본 과정)**

```python
# 파일 올리기 - 작업 디렉토리에서 GitHub까지
# ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
# │  작업 디렉토리   │      │  Staging Area   │      │  Local Repo     │      │     GitHub      │
# └─────────────────┘      └─────────────────┘      └─────────────────┘      └─────────────────┘
#         │                         │                         │                         │
#    [test.py 작성]                 │                         │                         │
#         │                         │                         │                         │
#         │── git add . ───────────>│                         │                         │
#         │                    [test.py 준비됨]               │                         │
#         │                         │                         │                         │
#         │                         │── git commit -m "..." ─>│                         │
#         │                         │                   [test.py 저장됨]                 │
#         │                         │                         │                         │
#         │                         │                         │── git push ───────────>│
#         │                         │                         │                   [GitHub에 업로드]
#         │                         │                         │                         │
#         ✅ 완료!
```

**명령어:**
```bash
git add .
git commit -m "test.py 추가"
git push
```

---

## 📥 **2. 파일 받기 (다운로드)**

```python
# 파일 받기 - GitHub에서 내 컴퓨터로
# ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
# │  작업 디렉토리   │      │  Staging Area   │      │  Local Repo     │      │     GitHub      │
# └─────────────────┘      └─────────────────┘      └─────────────────┘      └─────────────────┘
#         │                         │                         │                         │
#         │                         │                         │                   [새 파일 있음]
#         │                         │                         │                         │
#         │<────────────────────────────────────────────────── git pull ───────────────│
#    [새 파일 받음]                  │                         │                         │
#         │                         │                         │                         │
#         ✅ 완료!
```

**명령어:**
```bash
# 처음 받을 때 (최초 1회)
git clone https://github.com/아이디/저장소.git

# 업데이트 받을 때
git pull
```

---

## ❌ **3-A. Staging Area에서 취소 (add 취소)**

```python
# Staging Area에서 취소 - add를 되돌리기
# ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
# │  작업 디렉토리   │      │  Staging Area   │      │  Local Repo     │      │     GitHub      │
# └─────────────────┘      └─────────────────┘      └─────────────────┘      └─────────────────┘
#         │                         │                         │                         │
#    [test.py 수정함]                │                         │                         │
#         │                         │                         │                         │
#         │── git add . ───────────>│                         │                         │
#         │                    [test.py 준비됨]               │                         │
#         │                         │                         │                         │
#         │      실수 발견! 😱       │                         │                         │
#         │                         │                         │                         │
#         │<─── git reset ──────────│                         │                         │
#    [다시 돌아옴]             [준비 취소됨]                   │                         │
#    [파일은 그대로]                 │                         │                         │
#         │                         │                         │                         │
#         ✅ 완료! (파일 유지됨)
```

**명령어:**
```bash
# 모든 파일 add 취소
git reset

# 특정 파일만 add 취소
git reset test.py
```

---

## ❌ **3-B. 작업 디렉토리에서 취소 (수정 내용 버리기)**

```python
# 작업 디렉토리 변경사항 취소 - 수정 전으로 되돌리기
# ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
# │  작업 디렉토리   │      │  Staging Area   │      │  Local Repo     │      │     GitHub      │
# └─────────────────┘      └─────────────────┘      └─────────────────┘      └─────────────────┘
#         │                         │                         │                         │
#    [test.py 수정함]                │                         │                         │
#    [잘못 수정함! 😱]               │                         │                         │
#         │                         │                         │                         │
#         │<────────────────────── git checkout -- test.py ───│                         │
#    [수정 전으로 복구]              │                   [마지막 커밋 상태]              │
#    [⚠️ 수정 내용 사라짐]           │                         │                         │
#         │                         │                         │                         │
#         ✅ 완료! (수정 내용 삭제됨)
```

**명령어:**
```bash
# 특정 파일 수정 취소
git checkout -- test.py

# 모든 파일 수정 취소
git checkout .
```

**⚠️ 주의:** 수정한 내용이 **완전히 사라집니다!** 복구 불가!

---

## ❌ **3-C. Local Repo에서 취소 (커밋 취소 - 파일 유지)**

```python
# Local Repo에서 커밋 취소 - 파일은 유지
# ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
# │  작업 디렉토리   │      │  Staging Area   │      │  Local Repo     │      │     GitHub      │
# └─────────────────┘      └─────────────────┘      └─────────────────┘      └─────────────────┘
#         │                         │                         │                         │
#         │                         │                   [커밋 완료]                      │
#         │                         │                   [test.py 저장됨]                │
#         │                         │                         │                         │
#         │      커밋 메시지 오타! 😱 │                         │                         │
#         │                         │                         │                         │
#         │<────────────────────────────── git reset --soft HEAD~1 ────────────────────│
#         │                         │                   [커밋 취소됨]                    │
#    [파일 그대로]            [다시 준비됨]                    │                         │
#         │                         │                         │                         │
#         ✅ 완료! (파일 유지, 다시 커밋 가능)
```

**명령어:**
```bash
# 커밋만 취소 (파일 유지)
git reset --soft HEAD~1

# 다시 커밋
git commit -m "올바른 메시지"
```

---

## ❌ **3-D. Local Repo에서 완전 취소 (커밋+파일 모두 삭제)**

```python
# Local Repo에서 완전 취소 - 커밋도 파일도 모두 삭제
# ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
# │  작업 디렉토리   │      │  Staging Area   │      │  Local Repo     │      │     GitHub      │
# └─────────────────┘      └─────────────────┘      └─────────────────┘      └─────────────────┘
#         │                         │                         │                         │
#         │                         │                   [커밋 완료]                      │
#         │                         │                   [test.py 저장됨]                │
#         │                         │                         │                         │
#         │      다 필요없어! 😱       │                         │                         │
#         │                         │                         │                         │
#         │<────────────────────────────── git reset --hard HEAD~1 ────────────────────│
#    [파일 삭제됨]                   │                   [커밋 취소됨]                    │
#    [⚠️ 복구 불가!]                  │                   [이전 상태로]                    │
#         │                         │                         │                         │
#         ✅ 완료! (모든 변경사항 삭제됨)
```

**명령어:**
```bash
# 커밋+변경사항 모두 삭제
git reset --hard HEAD~1
```

**⚠️ 위험:** 모든 변경사항이 **완전히 사라집니다!** 복구 불가!

---

## 🗑️ **4-A. GitHub에서 파일 삭제 (로컬+GitHub 둘 다)**

```python
# GitHub에서 파일 삭제 - 로컬과 GitHub 모두 삭제
# ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
# │  작업 디렉토리   │      │  Staging Area   │      │  Local Repo     │      │     GitHub      │
# └─────────────────┘      └─────────────────┘      └─────────────────┘      └─────────────────┘
#         │                         │                         │                         │
#    [test.py 있음]                 │                         │                   [test.py 있음]
#         │                         │                         │                         │
#         │── git rm test.py ──────>│                         │                         │
#    [파일 삭제됨]             [삭제 준비됨]                   │                         │
#         │                         │                         │                         │
#         │                         │── git commit ──────────>│                         │
#         │                         │                   [삭제 저장됨]                     │
#         │                         │                         │                         │
#         │                         │                         │── git push ───────────>│
#         │                         │                         │                   [파일 삭제됨]
#         │                         │                         │                         │
#         ✅ 완료! (로컬+GitHub 모두 삭제)
```

**명령어:**
```bash
# 파일 삭제
git rm test.py

# 커밋
git commit -m "test.py 삭제"

# GitHub 반영
git push
```

---

## 🗑️ **4-B. GitHub만 삭제 (로컬 파일 유지)**

```python
# GitHub만 파일 삭제 - 로컬에는 파일 유지
# ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
# │  작업 디렉토리   │      │  Staging Area   │      │  Local Repo     │      │     GitHub      │
# └─────────────────┘      └─────────────────┘      └─────────────────┘      └─────────────────┘
#         │                         │                         │                         │
#    [test.py 있음]                 │                         │                   [test.py 있음]
#         │                         │                         │                         │
#         │── git rm --cached test.py ─>│                     │                         │
#    [파일 유지됨 ✅]          [삭제 준비됨]                   │                         │
#         │                         │                         │                         │
#         │                         │── git commit ──────────>│                         │
#         │                         │                   [삭제 저장됨]                     │
#         │                         │                         │                         │
#         │                         │                         │── git push ───────────>│
#         │                         │                         │                   [파일 삭제됨]
#    [로컬에는 있음 ✅]              │                         │                         │
#         │                         │                         │                         │
#         ✅ 완료! (GitHub만 삭제, 로컬 유지)
```

**명령어:**
```bash
# GitHub만 삭제 (로컬 유지)
git rm --cached test.py

# 커밋
git commit -m "GitHub에서만 test.py 삭제"

# GitHub 반영
git push
```

---

## 🔄 **5. 커밋 메시지만 수정**

```python
# 마지막 커밋 메시지 수정 - 내용은 그대로
# ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
# │  작업 디렉토리   │      │  Staging Area   │      │  Local Repo     │      │     GitHub      │
# └─────────────────┘      └─────────────────┘      └─────────────────┘      └─────────────────┘
#         │                         │                         │                         │
#         │                         │                   [커밋: "Day 2 완뢰"]            │
#         │                         │                   [메시지 오타! 😱]               │
#         │                         │                         │                         │
#         │───────────────────────────── git commit --amend -m "Day 2 완료" ───────────>│
#         │                         │                   [메시지 수정됨 ✅]               │
#         │                         │                   [파일은 그대로]                  │
#         │                         │                         │                         │
#         │                         │                         │── git push ───────────>│
#         │                         │                         │                   [수정 반영됨]
#         │                         │                         │                         │
#         ✅ 완료! (메시지만 수정)
```

**명령어:**
```bash
# 마지막 커밋 메시지 수정
git commit --amend -m "올바른 메시지"

# GitHub 반영
git push
```

---

## 📋 **전체 상황별 명령어 요약**

| 상황 | 위치 | 명령어 | 파일 상태 |
|------|------|--------|-----------|
| 📤 **올리기** | 작업 → GitHub | `git add . → commit → push` | 유지 |
| 📥 **받기** | GitHub → 작업 | `git pull` | 추가됨 |
| ❌ **add 취소** | Staging → 작업 | `git reset` | 유지 |
| ❌ **수정 취소** | 작업 디렉토리 | `git checkout -- 파일` | 복구 (⚠️ 수정 삭제) |
| ❌ **커밋 취소 (유지)** | Local Repo | `git reset --soft HEAD~1` | 유지 |
| ❌ **커밋 취소 (삭제)** | Local Repo | `git reset --hard HEAD~1` | 삭제 (⚠️ 복구 불가) |
| 🗑️ **파일 삭제** | 로컬+GitHub | `git rm 파일 → commit → push` | 삭제 |
| 🗑️ **GitHub만 삭제** | GitHub만 | `git rm --cached 파일 → commit → push` | 로컬 유지 |
| 🔄 **메시지 수정** | Local Repo | `git commit --amend -m "..."` | 유지 |

---
